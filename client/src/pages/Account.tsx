import { useAuth } from "@/_core/hooks/useAuth";
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar";
import { Card } from "@/components/ui/card";
import { appRoutes } from "@/lib/appRoutes";
import { trpc } from "@/lib/trpc";
import { BadgeCheck, CircleUserRound, Mail, ShieldCheck, UserRound } from "lucide-react";

export default function Account() {
  const { user } = useAuth();
  const { data: profile } = trpc.account.profile.useQuery();
  const initial = (profile?.name || user?.name || "T").charAt(0).toUpperCase();

  return <div className="min-h-full bg-[#07101f] px-5 py-8 text-white sm:px-7 lg:px-10"><div className="mx-auto max-w-5xl"><div className="mb-7"><p className="font-mono text-[10px] uppercase tracking-[0.22em] text-[oklch(0.70_0.16_250)]">Member account</p><h1 className="mt-2 text-3xl font-semibold tracking-[-0.04em] text-white">Your trader profile</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-400">Your account stays private. Your profile image is linked to the email address used when you sign in.</p></div><div className="grid gap-5 lg:grid-cols-[1.15fr_0.85fr]"><Card className="border-blue-200/[0.10] bg-gradient-to-b from-[#152647] to-[#101c33] p-5 sm:p-6"><div className="flex flex-col gap-5 sm:flex-row sm:items-center"><Avatar className="h-20 w-20 border-2 border-blue-300/[0.22] bg-slate-800 shadow-[0_0_26px_oklch(0.66_0.18_250_/_0.22)]"><AvatarImage src={profile?.avatarUrl ?? undefined} alt={`${profile?.name || "Trader"} profile`} /><AvatarFallback className="bg-slate-800 text-lg text-blue-200">{initial}</AvatarFallback></Avatar><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h2 className="truncate text-xl font-semibold text-white">{profile?.name || "Trader"}</h2>{profile?.role === "admin" && <span className="inline-flex items-center gap-1 rounded-full border border-blue-300/[0.20] bg-blue-400/[0.10] px-2 py-1 font-mono text-[9px] uppercase tracking-[0.12em] text-blue-100"><ShieldCheck className="h-3 w-3" />Founder · Moderator</span>}</div><p className="mt-2 flex items-center gap-2 text-sm text-slate-400"><Mail className="h-4 w-4 text-slate-500" />{profile?.email || "Email unavailable"}</p><p className="mt-2 text-xs leading-5 text-slate-500">Profile photo is provided through your email-linked avatar when available. Otherwise, Trade Fusion uses a private initials fallback.</p></div></div></Card><Card className="border-blue-200/[0.10] bg-[#101c33] p-5"><p className="font-mono text-[9px] uppercase tracking-[0.18em] text-slate-500">Account privacy</p><div className="mt-4 space-y-3 text-sm"><div className="flex gap-3"><ShieldCheck className="mt-0.5 h-4 w-4 shrink-0 text-blue-300" /><span className="text-slate-300">Your journal and account information remain private.</span></div><div className="flex gap-3"><BadgeCheck className="mt-0.5 h-4 w-4 shrink-0 text-blue-300" /><span className="text-slate-300">Only the trading style you select is shown in Trader’s Room.</span></div></div></Card></div><Card className="mt-5 border-blue-200/[0.10] bg-[#101c33] p-5 sm:p-6"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl border border-blue-200/[0.12] bg-blue-400/[0.08] text-blue-200"><UserRound className="h-4 w-4" /></span><div><p className="text-sm font-medium text-white">Profile photo and sign-in email</p><p className="mt-1 text-xs leading-5 text-slate-500">The account details above are based on your current authenticated sign-in. Updating your identity provider profile will be reflected after a fresh sign-in.</p></div></div></Card></div></div>;
}
