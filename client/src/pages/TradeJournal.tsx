import React, { useEffect, useMemo, useState } from "react";
import { BookOpenCheck, BrainCircuit, Check, ChevronRight, ClipboardPenLine, LockKeyhole, Save, Star } from "lucide-react";
import { toast } from "sonner";
import { trpc } from "@/lib/trpc";

type JournalForm = { tradeIdea: string; marketContext: string; executionReview: string; reflection: string; emotion: string; rating: number | null };
const emptyForm: JournalForm = { tradeIdea: "", marketContext: "", executionReview: "", reflection: "", emotion: "", rating: null };
function money(value: number) { return new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 2 }).format(value); }

export default function TradeJournal() {
  const { data: trades = [] } = trpc.trades.list.useQuery();
  const [selectedTradeId, setSelectedTradeId] = useState<number | null>(null);
  const [form, setForm] = useState<JournalForm>(emptyForm);
  const input = useMemo(() => ({ tradeId: selectedTradeId ?? 0 }), [selectedTradeId]);
  const { data: entry, isLoading } = trpc.tradeJournal.byTrade.useQuery(input, { enabled: selectedTradeId !== null });
  const { data: savedEntries = [] } = trpc.tradeJournal.list.useQuery();
  const utils = trpc.useUtils();
  const upsert = trpc.tradeJournal.upsert.useMutation({ onSuccess: () => { utils.tradeJournal.byTrade.invalidate(); utils.tradeJournal.list.invalidate(); toast.success("Private Journal entry saved."); } });
  const remove = trpc.tradeJournal.delete.useMutation({ onSuccess: () => { utils.tradeJournal.byTrade.invalidate(); utils.tradeJournal.list.invalidate(); setForm(emptyForm); toast.success("Journal entry cleared."); } });
  useEffect(() => { if (!selectedTradeId && trades.length) setSelectedTradeId(trades[0].id); }, [selectedTradeId, trades]);
  useEffect(() => { setForm(entry ? { tradeIdea: entry.tradeIdea, marketContext: entry.marketContext, executionReview: entry.executionReview, reflection: entry.reflection, emotion: entry.emotion, rating: entry.rating } : emptyForm); }, [entry, selectedTradeId]);
  const selected = useMemo(() => trades.find(trade => trade.id === selectedTradeId) ?? null, [trades, selectedTradeId]);
  const setField = <K extends keyof JournalForm>(field: K, value: JournalForm[K]) => setForm(current => ({ ...current, [field]: value }));
  const save = () => { if (!selectedTradeId) return; upsert.mutate({ tradeId: selectedTradeId, ...form }); };
  return <div className="min-h-full bg-[#06090f] text-foreground"><main className="mx-auto w-full max-w-[1720px] px-4 py-5 sm:px-5 lg:px-7 lg:py-6"><section className="mb-5 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="font-mono text-[9px] uppercase tracking-[.22em] text-violet-300">Private trade notes</p><h1 className="mt-1.5 text-2xl font-semibold tracking-[-.04em] text-white sm:text-3xl">Journal</h1><p className="mt-1.5 text-xs text-slate-500">Capture an idea, context, execution review, and learning for each live trade.</p></div><div className="inline-flex items-center gap-2 self-start rounded-full border border-emerald-400/15 bg-emerald-500/[.06] px-3 py-1.5 font-mono text-[9px] uppercase tracking-[.14em] text-emerald-200"><LockKeyhole className="h-3.5 w-3.5" />Private to your account</div></section>{!trades.length ? <section className="rounded-2xl border border-dashed border-white/[.08] bg-[#0a111f] py-20 text-center"><BookOpenCheck className="mx-auto h-7 w-7 text-slate-700" /><p className="mt-4 text-base font-semibold text-white">Log a live trade before adding a Journal entry.</p><p className="mt-2 text-xs text-slate-500">Journal ideas and reviews are always linked to a live trade you own.</p></section> : <section className="grid gap-4 xl:grid-cols-[310px_minmax(0,1fr)]"><aside className="rounded-2xl border border-white/[.07] bg-[#0a111f] p-3 sm:p-4"><div className="flex items-center justify-between px-1 pb-3"><div><p className="font-mono text-[9px] uppercase tracking-[.16em] text-slate-500">Trade queue</p><p className="mt-1 text-xs font-medium text-white">Select an execution</p></div><span className="rounded-md bg-violet-500/[.1] px-2 py-1 font-mono text-[9px] text-violet-200">{savedEntries.length} WRITTEN</span></div><div className="max-h-[560px] space-y-2 overflow-y-auto pr-1">{[...trades].sort((a, b) => b.date.localeCompare(a.date)).map(trade => { const written = savedEntries.some(item => item.tradeId === trade.id); return <button type="button" key={trade.id} onClick={() => setSelectedTradeId(trade.id)} className={`tf-press w-full rounded-xl border p-3 text-left transition ${trade.id === selectedTradeId ? "border-violet-300/30 bg-violet-500/[.1]" : "border-white/[.055] bg-white/[.018] hover:bg-white/[.04]"}`}><div className="flex items-center justify-between gap-3"><span className="font-mono text-xs font-semibold text-white">{trade.symbol}</span><span className={`font-mono text-[11px] ${trade.pnl > 0 ? "text-emerald-300" : trade.pnl < 0 ? "text-rose-300" : "text-slate-400"}`}>{money(trade.pnl)}</span></div><p className="mt-1 text-[10px] text-slate-500">{trade.date} · {trade.direction} {trade.setupTag ? `· ${trade.setupTag}` : ""}</p><span className={`mt-2 inline-flex items-center gap-1 font-mono text-[9px] uppercase tracking-[.1em] ${written ? "text-emerald-300" : "text-slate-600"}`}>{written ? <><Check className="h-3 w-3" /> Review saved</> : "No Journal entry"}</span></button>; })}</div></aside><section className="min-w-0 rounded-2xl border border-white/[.07] bg-[#0a111f] p-4 shadow-[0_18px_38px_rgba(0,0,0,.2)] sm:p-5">{selected ? <><div className="flex flex-col gap-3 border-b border-white/[.07] pb-4 sm:flex-row sm:items-start sm:justify-between"><div className="flex items-start gap-3"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl border border-violet-400/15 bg-violet-500/[.07]"><ClipboardPenLine className="h-5 w-5 text-violet-300" /></div><div><p className="font-mono text-[9px] uppercase tracking-[.16em] text-slate-500">Linked live trade</p><h2 className="mt-1 text-lg font-semibold text-white">{selected.symbol} <span className="font-mono text-xs font-normal text-slate-500">· {selected.direction} · {selected.date}</span></h2><p className="mt-1 text-xs text-slate-500">{selected.setupTag || "No setup label"} · <span className={selected.pnl > 0 ? "text-emerald-300" : selected.pnl < 0 ? "text-rose-300" : "text-slate-400"}>{money(selected.pnl)} live result</span></p></div></div><span className="font-mono text-[9px] text-slate-600">{isLoading ? "Loading…" : entry ? "Last saved privately" : "New private entry"}</span></div><div className="mt-5 grid gap-4 lg:grid-cols-2"><label className="block"><span className="font-mono text-[9px] uppercase tracking-[.15em] text-violet-200">Trade idea</span><textarea value={form.tradeIdea} onChange={event => setField("tradeIdea", event.target.value)} placeholder="What was the thesis, level, or trigger?" className="mt-2 min-h-32 w-full resize-y rounded-xl border border-white/[.08] bg-[#070d18] p-3 text-sm leading-6 text-slate-200 placeholder:text-slate-600 focus:border-violet-300/40 focus:outline-none" /></label><label className="block"><span className="font-mono text-[9px] uppercase tracking-[.15em] text-blue-200">Market context</span><textarea value={form.marketContext} onChange={event => setField("marketContext", event.target.value)} placeholder="Session, structure, macro risk, or key levels." className="mt-2 min-h-32 w-full resize-y rounded-xl border border-white/[.08] bg-[#070d18] p-3 text-sm leading-6 text-slate-200 placeholder:text-slate-600 focus:border-blue-300/40 focus:outline-none" /></label><label className="block"><span className="font-mono text-[9px] uppercase tracking-[.15em] text-amber-200">Execution review</span><textarea value={form.executionReview} onChange={event => setField("executionReview", event.target.value)} placeholder="What was executed well? Where did you deviate?" className="mt-2 min-h-32 w-full resize-y rounded-xl border border-white/[.08] bg-[#070d18] p-3 text-sm leading-6 text-slate-200 placeholder:text-slate-600 focus:border-amber-300/40 focus:outline-none" /></label><label className="block"><span className="font-mono text-[9px] uppercase tracking-[.15em] text-emerald-200">Reflection</span><textarea value={form.reflection} onChange={event => setField("reflection", event.target.value)} placeholder="What should be repeated or changed next time?" className="mt-2 min-h-32 w-full resize-y rounded-xl border border-white/[.08] bg-[#070d18] p-3 text-sm leading-6 text-slate-200 placeholder:text-slate-600 focus:border-emerald-300/40 focus:outline-none" /></label></div><div className="mt-4 flex flex-col gap-4 rounded-xl border border-white/[.06] bg-white/[.018] p-3.5 sm:flex-row sm:items-end sm:justify-between"><label className="block w-full sm:max-w-xs"><span className="font-mono text-[9px] uppercase tracking-[.15em] text-slate-500">Mindset label</span><input value={form.emotion} onChange={event => setField("emotion", event.target.value)} placeholder="Focused, patient, rushed…" className="mt-2 h-10 w-full rounded-lg border border-white/[.08] bg-[#070d18] px-3 text-sm text-slate-200 placeholder:text-slate-600 focus:outline-none focus:ring-1 focus:ring-violet-300" /></label><div><p className="font-mono text-[9px] uppercase tracking-[.15em] text-slate-500">Process rating</p><div className="mt-2 flex items-center gap-1">{[1, 2, 3, 4, 5].map(rating => <button type="button" key={rating} onClick={() => setField("rating", form.rating === rating ? null : rating)} aria-label={`Set process rating to ${rating}`} className={`tf-press grid h-8 w-8 place-items-center rounded-lg border ${form.rating && rating <= form.rating ? "border-amber-300/30 bg-amber-500/[.12] text-amber-200" : "border-white/[.08] text-slate-600"}`}><Star className="h-3.5 w-3.5" /></button>)}</div></div></div><div className="mt-5 flex flex-wrap items-center justify-between gap-3"><p className="inline-flex items-center gap-2 text-xs text-slate-500"><BrainCircuit className="h-4 w-4 text-violet-300" />Ideas are private and never posted to Trader’s Room.</p><div className="flex gap-2">{entry && <button type="button" onClick={() => remove.mutate({ id: entry.id })} className="tf-press rounded-lg border border-rose-400/15 px-3 py-2 text-xs text-rose-300 hover:bg-rose-500/[.08]">Clear entry</button>}<button type="button" onClick={save} disabled={upsert.isPending} className="tf-press inline-flex items-center gap-1.5 rounded-lg bg-violet-500 px-3.5 py-2 text-xs font-semibold text-white hover:bg-violet-400 disabled:opacity-60"><Save className="h-3.5 w-3.5" />{upsert.isPending ? "Saving…" : "Save private Journal"}</button></div></div></> : null}</section></section>}</main></div>;
}
